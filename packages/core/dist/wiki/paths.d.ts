import type { WikiCategory } from '@lumemex/shared';
export declare function wikiPagePath(wikiDir: string, category: WikiCategory, slug: string): string;
export declare function ensureWikiCategoryDir(wikiDir: string, category: WikiCategory): string;
